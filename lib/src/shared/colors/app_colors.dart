import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Colors.white;
  static const Color theme = Colors.deepPurple;
  static final Color themeLight = Colors.deepPurple.withAlpha(50);
}
